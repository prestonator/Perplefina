services:
  searxng:
    image: docker.io/searxng/searxng:latest
    volumes:
      - ./:/etc/searxng:rw
    ports:
      - 4000:8080
    networks:
      - perplexica-network
    restart: unless-stopped

networks:
  perplexica-network:

volumes:
  backend-dbstore:
  uploads: